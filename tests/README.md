# Tests

Run all tests:

```bash
python -m pytest tests -q
```

Run one file:

```bash
python -m pytest tests/test_routes.py -q
```

Test suite includes:
- model tests
- auth tests
- route tests
- API tests
